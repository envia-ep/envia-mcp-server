# Envia API token (required)
# Get yours at https://app.envia.com → Settings → API Keys (sandbox)
# or https://app-production.envia.com → Settings → API Keys (production)
ENVIA_API_KEY=your_jwt_token_here

# Environment: "sandbox" (default) or "production"
ENVIA_ENVIRONMENT=sandbox
